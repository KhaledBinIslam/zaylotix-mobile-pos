*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
# NOT ignoring /public/build (see below) — deliberately committed so
# shared hosting without Node/npm can deploy with a plain `git pull`,
# never needing to run a frontend build on the server.
/public/hot
/public/storage
/database/database.sqlite
/byapari
/storage/app/private/backups
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

# Android/Capacitor build output — regenerated by `npx cap sync` + gradle,
# not source; keeping these out avoids committing gigabytes of build cache
/android/build
/android/app/build
/android/.gradle
/android/capacitor-cordova-android-plugins/build
/android/local.properties
/*.apk
